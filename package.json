{
  "name": "apollo-react-commit-benchmark",
  "version": "1.0.0",
  "private": true,
  "packageManager": "bun@1.3.6",
  "scripts": {
    "build": "bun build src/benchmark.ts --outfile benchmark.js --target browser --format esm",
    "check": "bun run build && bun scripts/check-content.ts",
    "dev": "bun x serve ."
  }
}
